describe('instagramTagService', function() {

  beforeEach(module('instagramModule'));

  it('should ...', inject(function(instagramTagService) {

    //expect(instagramTagService.doSomething()).toEqual('something');

  }));

});
