describe('instagramFactory', function() {

  beforeEach(module('instagramModule'));

  it('should ...', inject(function(instagramFactory) {

    //expect(instagramFactory.doSomething()).toEqual('something');

  }));

});
