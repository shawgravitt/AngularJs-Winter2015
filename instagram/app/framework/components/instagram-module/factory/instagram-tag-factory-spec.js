describe('instagramTagFactory', function() {

  beforeEach(module('instagramModule'));

  it('should ...', inject(function(instagramTagFactory) {

    //expect(instagramTagFactory.doSomething()).toEqual('something');

  }));

});
