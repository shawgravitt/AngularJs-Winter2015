describe('strainsFactory', function() {

  beforeEach(module('strainsModule'));

  it('should ...', inject(function(strainsFactory) {

    //expect(strainsFactory.doSomething()).toEqual('something');

  }));

});
