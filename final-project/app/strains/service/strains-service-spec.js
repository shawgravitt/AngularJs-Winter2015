describe('strainsService', function() {

  beforeEach(module('strainsModule'));

  it('should ...', inject(function(strainsService) {

    //expect(strainsService.doSomething()).toEqual('something');

  }));

});
