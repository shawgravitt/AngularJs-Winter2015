describe('navigationService', function() {

  beforeEach(module('navigationModule'));

  it('should ...', inject(function(navigationService) {

    //expect(navigationService.doSomething()).toEqual('something');

  }));

});
